management:
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-components: always
    # show-details: always

info:
  app:
    name: spring-actuator
    company: lcb

logging:
  level:
    com.lee.actuator: debug

server:
  tomcat:
    mbeanregistry:
      enabled: true